--- coreutils-6.10/src/ls.c.bak	2008-04-01 06:56:38.222851023 -0400
+++ coreutils-6.10/src/ls.c	2008-04-01 06:58:16.654848696 -0400
@@ -2668,7 +2668,7 @@
 	  err = (attr_len < 0);
 
 	  if (err == 0 && f->scontext != NULL)
-	    have_acl = ! STREQ ("unlabeled", f->scontext);
+	    ; /* for now, we're ignoring selinux contexts */
 	  else
 	    {
 	      f->scontext = UNKNOWN_SECURITY_CONTEXT;
